Ua los test que empiezan con dynamic para comprobar la practica una vez que se haya creado el API REST. Usa los test que empiezan por static en los demás casos.
Para ejecutar los test debes tener en cuenta las siguientes consideraciones:

0) El contenido de este repositorio debe copiarse en la raíz de vuestro projecto vue.
1) En el fichero cypress.config.js están definidas varias variables como "baseUrl" que contiene la dirección donde se encuentra el servidor de vue 
   (normalmente http://localhost:5173)
2) Los test hacen un uso intensivo de las atributos "data-cy". Por ejemplo, la linea

          cy.get('[data-cy=username]').type(Cypress.env('username'));

  buscará el atributo "data-cy=username" en vuestro fichero vue y escribirá en el campo asociado 
el valor de la variable "username" definido en "cypress.config.js"

Así pues xxxxx.vue debera tener un campo input (donde se teclea el username) parecido a

                                           <input name="username" 
                                                   ... 
                                                   data-cy="username"
                                                   ... />
esto es, conteniendo el atributo 'data-cy="username"'
